REM Optional. The script normally prompts for the token and saves it to
REM %USERPROFILE%\.finhq_token. To use an env var instead, set FINHQ_TOKEN.
REM Get the fhq_ token from the FinHQ app > MCP/Connect. NEVER commit a real token.
FINHQ_TOKEN=fhq_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
